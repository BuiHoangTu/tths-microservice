server:
  port: 0

spring:
  application:
    name: pse-household-service


logging:
  rootCategory: INFO, consoleAppender, fileAppender

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka